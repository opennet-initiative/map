This package contains configuration files for apache2 for serving static files.

Enable the apache2 site:
 a2ensite on-map.conf
 service apache2 restart

Probably you will want to reconfigure the ServerName and the URL of the
Geronimo API in /etc/apache2/sites-available/on-map.conf.
